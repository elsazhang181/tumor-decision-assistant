# Coze 工作流校验节点配置指南

## 问题说明

健康就医决策助手在回复"南京职工医保报销比例和北京职工医保报销比例对比"时，给出了非常具体的数字（如"社区医院起付线 200 元，报销比例 80%"），但这些数字可能不是来自知识库，而是模型编造的。

对比蚂蚁阿福的回复，后者更加谨慎，明确标注了信息来源，对于不确定的信息会说"现有资料未给出"。

## 解决方案

在 Coze 工作流中添加一个**校验节点**，验证 Bot 回复中的数字是否都出现在检索到的知识库片段中。如果校验失败，自动改口为"当前资料无法确认"。

## 配置步骤

### 第一步：进入工作流编辑

1. 登录 Coze 平台
2. 进入「健康就医决策助手」Bot 编辑页面
3. 点击左侧菜单的「工作流」
4. 创建新工作流或编辑现有工作流

### 第二步：添加代码节点

1. 在工作流画布中，点击「+」添加节点
2. 选择「代码」节点
3. 节点名称：`引用校验`
4. 语言选择：`Python`

### 第三步：配置输入参数

在代码节点的输入配置中，添加以下参数：

| 参数名 | 类型 | 说明 | 来源 |
|-------|------|------|------|
| `answer` | String | Bot 生成的回答 | 上游 LLM 节点的输出 |
| `retrieved_chunks` | Array<String> | 检索到的知识库片段 | 上游知识库检索节点的输出 |

### 第四步：粘贴校验代码

将以下代码粘贴到代码节点的编辑器中：

```python
import re
from typing import Tuple, List

def verify_citation(answer: str, retrieved_chunks: List[str]) -> Tuple[bool, str]:
    """
    校验回答中的数字是否都出现在检索原文中
    """
    # 提取回答中的所有数字（包括带单位的）
    numbers_in_answer = set(re.findall(r'\d+\.?\d*\s*[万元%]?', answer))
    
    # 提取所有检索片段中的数字
    numbers_in_chunks = set()
    for chunk in retrieved_chunks:
        numbers_in_chunks.update(re.findall(r'\d+\.?\d*\s*[万元%]?', chunk))
    
    # 找出回答中有但原文中没有的数字
    suspicious = numbers_in_answer - numbers_in_chunks
    
    if suspicious:
        return False, f"回答中以下数字未在引用来源中找到：{suspicious}"
    
    return True, "ok"

def main(answer: str, retrieved_chunks: List[str]) -> dict:
    """
    Coze 工作流代码节点主函数
    """
    verified, message = verify_citation(answer, retrieved_chunks)
    
    if verified:
        return {
            "verified": True,
            "message": "校验通过",
            "final_answer": answer
        }
    else:
        fallback = "当前资料无法确认该信息，建议通过官方渠道查询或咨询医保部门。"
        return {
            "verified": False,
            "message": message,
            "final_answer": fallback
        }
```

### 第五步：配置输出参数

在代码节点的输出配置中，添加以下参数：

| 参数名 | 类型 | 说明 |
|-------|------|------|
| `verified` | Boolean | 是否通过校验 |
| `message` | String | 校验结果消息 |
| `final_answer` | String | 最终回答（通过校验则为原回答，失败则为备用回答） |

### 第六步：连接节点

工作流结构应该如下：

```
用户输入 → 知识库检索 → LLM 生成回答 → 引用校验 → 输出最终回答
```

具体连接：
1. **知识库检索节点** 的输出 `chunks` → **引用校验节点** 的输入 `retrieved_chunks`
2. **LLM 节点** 的输出 `answer` → **引用校验节点** 的输入 `answer`
3. **引用校验节点** 的输出 `final_answer` → **最终输出**

### 第七步：添加条件分支（可选）

如果需要更复杂的逻辑，可以添加条件分支：

```
引用校验节点
    ↓
条件判断：verified == True?
    ├─ 是 → 输出原回答
    └─ 否 → 输出"当前资料无法确认"
```

## 测试验证

配置完成后，测试以下问题：

### 测试 1：知识库中有明确信息
- 问题："北京安宁疗护机构有哪些"
- 预期：正常回复，列出知识库中的机构

### 测试 2：知识库中信息不明确
- 问题："南京职工医保报销比例和北京职工医保报销比例对比"
- 预期：如果知识库中没有明确的对比数据，回复"当前资料无法确认该信息"

### 测试 3：数字校验
- 问题："化疗血小板减少怎么办"
- 预期：回复中的数字必须与知识库原文完全一致

## 注意事项

1. **知识库必须正确关联**：确保 Bot 已关联相关知识库
2. **检索节点必须配置**：确保工作流中有知识库检索节点
3. **LLM 节点输出格式**：确保 LLM 节点的输出包含 `answer` 字段
4. **代码节点超时**：如果回答很长，可能需要增加代码节点的超时时间

## 备选方案：在提示词中强化自我校验

如果工作流配置复杂，可以在提示词中强化自我校验规则（已更新到 `assets/coze-bot-prompt.md`）：

```
**【自我校验规则 - 强制执行】**
在生成回复前，必须在内部执行以下校验（不输出给用户）：
1. 提取回复中的所有数字（金额、百分比、日期等）
2. 检查这些数字是否都出现在检索到的知识库片段中
3. 如果有任何数字不在知识库原文中，**必须**改口为："当前资料无法确认该信息，建议通过官方渠道查询。"
4. **绝对禁止**输出校验过程，只输出最终结果
```

## 预期效果

配置校验节点后，Bot 的回复应该更加准确：

**之前（可能编造数据）**：
```
南京门诊：社区医院起付线 200 元，报销比例 80%
```

**之后（校验失败时）**：
```
当前资料无法确认该信息，建议通过官方渠道查询或咨询医保部门。
```

**之后（校验通过时）**：
```
根据知识库信息：
[原样复制知识库原文，数字完全一致]
```
